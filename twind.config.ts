import { Options } from "$fresh/plugins/twind.ts";

export default {
  selfURL: import.meta.url,
  theme: {
    colors: {
      base: "#f8f3de",
      primary: "#d4dfbb",
      secondary: "#e5af9b",
      white: "#ffffff",
    },
  },
} as Options;
